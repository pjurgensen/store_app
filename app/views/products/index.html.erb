<%= content_for(:title, "Products") %>

<div class="all-product container">

  <h1>Awesome Apparel</h1>

  <ul>
    <% @products.each do |product| %>
      <li><%= link_to product.name, product_path(product) %></li>
    <% end %>
  </ul>

  <p> <%= link_to 'Create New Product', new_product_path %>
  </p>

</div>
